TODO List – Reestruturação do Pipeline
🔨 1. Criar o novo workflow unificado
Criar .github/workflows/verificar_tudo.yml com o conteúdo final ajustado:

on: apenas com push (paths dados/*.json, apostas/*.json) + workflow_dispatch.

Removido completamente workflow_run.

Sintaxe correta de paths (com ! para exclusões):

yaml
push:
  paths:
    - 'dados/*.json'
    - 'apostas/*.json'
    - '!resultados/**'
    - '!notificacoes_*.json'
    - '!estatisticas_completas.json'
concurrency: group: repo-write, cancel-in-progress: false

defaults.run.shell: bash

Passos de verificação com continue-on-error: true (Totoloto, Euromilhões, EuroDreams, M1lhão).

Após verificações, correr gerar_notificacoes.py e gerar_estatisticas_completas.py.

Bloco de commit robusto:

git pull origin main --rebase antes de gerar qualquer coisa.

git stash -u para preservar ficheiros untracked.

git stash pop com verificação de conflito (exit 1 se houver conflito real).

git add resultados/ notificacoes_ativas.json notificacoes_historico.json estatisticas_completas.json

git commit e git push (só o push com retry).

✏️ 2. Modificar workflows existentes
ocr.yml:

Adicionar concurrency: group: repo-write, cancel-in-progress: false.

Substituir o passo de limpeza de imagens por mover para uploads/processadas/YYYY-MM/.

Remover [skip ci] da mensagem de commit.

Aplicar o mesmo padrão robusto de commit (pull antes, retry só no push).

atualizar_totoloto_sc.yml, atualizar_euromilhoes_sc.yml, atualizar_eurodreams_sc.yml, atualizar_milhao_sc.yml:

Adicionar concurrency: group: repo-write, cancel-in-progress: false.

(Opcional) Ajustar o commit para o mesmo padrão seguro.

verificar-validacoes-diario.yml:

Adicionar concurrency: group: repo-write, cancel-in-progress: false.

🗑️ 3. Remover workflows obsoletos
Após confirmar que o novo verificar_tudo.yml funciona corretamente, desativar/apagar os seguintes ficheiros:

verificar_totoloto.yml

verificar_euromilhoes.yml

verificar_eurodreams.yml

verificar_milhao.yml

detectar_resultados.yml

atualizar_estatisticas.yml

gera_estatisticas.yml

(O workflow reset.yml pode ser mantido para emergências.)

⚙️ 4. Ajustes de configuração do repositório
Verificar se as permissões dos workflows estão como Read and write (Settings → Actions → General → Workflow permissions).

Confirmar que o token usado (secrets.GITHUB_TOKEN) tem permissão contents: write em cada workflow que faz push.

✅ 5. Testes e validação
Fazer um upload real de uma foto e acompanhar o pipeline:

OCR deve correr, mover a imagem e commitar.

Verificar Tudo deve disparar automaticamente e gerar resultados/notificações/estatísticas num só commit.

Não podem ocorrer execuções duplicadas nem conflitos de push.

Verificar que as notificações e estatísticas aparecem corretamente na SPA.

Deixar o sistema funcionar por 1-2 dias com os workflows antigos desativados (não apagados) para garantia.

📊 Resumo do impacto
Métrica	Antes	Depois
Workflows ativos	15	8
Execuções por upload	~20	2
Pages builds por upload	múltiplos	2
Conflitos Git	frequentes	zero
Imagens órfãs	sim	não
Manutenção	complexa	linear
