name: Atualizar EuroDreams automaticamente

# horario: inverno

on:
  schedule:
    # Segunda
    - cron: "0 21 * * 1"
    - cron: "0 22 * * 1"
    - cron: "0 23 * * 1"
    - cron: "0 10 * * 2"   # backup terça

    # Quinta
    - cron: "0 21 * * 4"
    - cron: "0 22 * * 4"
    - cron: "0 23 * * 4"
    - cron: "0 10 * * 5"   # backup sexta

  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Instalar Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Instalar dependências Python
        run: |
          pip install selenium chromedriver-autoinstaller

      - name: Rodar script de scraping
        run: python scripts/atualizar_eurodreams_sc.py

      - name: Commit e push das alterações
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dados/
          git add logs/
          if ! git diff --cached --quiet; then
            git commit -m "Atualização automática EuroDreams"
            git push
          else
            echo "Nenhuma alteração, nada para commitar."
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
